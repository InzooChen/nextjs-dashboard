import { Inconsolata, Inter, Lusitana } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const inconsolata = Inconsolata({ subsets: ['latin'] });

export const lusitana = Inconsolata({ subsets: ['latin'] });
